# Carpeta intermedia `NPS-System`

El **README principal** del entregable está en la **raíz del repositorio** (`../README.md`): allí están las instrucciones completas para evaluadores y para levantar BD, API y Angular.

El código del ejercicio vive en la subcarpeta **`NPS-System/`** (solución `NPS.sln`, `frontend/`, `database/`, `src/`).
